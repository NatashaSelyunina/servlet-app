name: Deploy to Jenkins

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        run: |
          curl -X POST "http://192.168.0.101:8081/job/DeployServlet/build?token=401c0dd28028aa9e6cafc6bf1646952f"